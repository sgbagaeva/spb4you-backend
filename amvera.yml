---
version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: ./gradlew clean build -x test --no-daemon
  args: null
  persistenceMount: /data
  containerPort: 80
