version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
  command: "./gradlew clean build -x test --no-daemon"
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "8080"  # ← ваш порт в контейнере
  servicePort: "80"
