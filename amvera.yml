meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
  command: "./gradlew clean build -x test --no-daemon"
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: 80
